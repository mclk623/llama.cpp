name: Windows CUDA Build Only

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths: [
      '.github/workflows/release.yml',
      '**/CMakeLists.txt',
      '**/.cmake',
      '**/*.h',
      '**/*.hpp',
      '**/*.c',
      '**/*.cpp',
      '**/*.cu',
      '**/*.cuh',
      '**/*.swift',
      '**/*.m',
      '**/*.metal',
      '**/*.comp',
      '**/*.glsl'
    ]

env:
  GH_TOKEN: ${{ github.token }}
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
  CMAKE_ARGS: "-DLLAMA_BUILD_EXAMPLES=OFF -DLLAMA_BUILD_TESTS=OFF -DLLAMA_BUILD_TOOLS=ON -DLLAMA_BUILD_SERVER=ON -DGGML_RPC=ON"

concurrency:
  group: release
  queue: max

jobs:
  hip:
    runs-on: windows-2022

    permissions:
      actions: write

    env:
      # Make sure this is in sync with build-cache.yml
      ROCM_VERSION: "7.14.0"

    strategy:
      matrix:
        include:
          # sync with release.yml
          - ROCM_VERSION: "7.14.0"
            gpu_targets: "gfx1011"
            build: x64
            name: "radeon"

    steps:
      - name: Clone
        id: checkout
        uses: actions/checkout@v6

      # - name: Cache ROCm Installation
      #   uses: actions/cache@v5
      #   id: cache-rocm
      #   with:
      #     path: C:\TheRock\build
      #     key: rocm-wheels-${{ env.ROCM_VERSION }}-multi-arch-${{ runner.os }}


      - name: Install Ninja
        run: |
          choco install ninja
          
      - name: Download and extract TheRock gfx101X tarball
        run: |
          $ErrorActionPreference = "Stop"
          mkdir D:\therock-gfx101x
          cd D:\therock-gfx101x
          
          curl -o therock.tar.gz https://therock-nightly-tarball.s3.amazonaws.com/therock-dist-windows-gfx101X-dgpu-7.14.0a20260612.tar.gz
          
          tar -xzf therock.tar.gz
          
          if (-not (Test-Path "bin\rocblas.dll")) {
            throw "rocblas.dll not found in tarball"
          }
          
          Write-Host "TheRock tarball extracted successfully"

      - name: Setup TheRock Environment
        run: |
          $therockPath = "D:\therock-gfx101x"
          
          echo "HIP_PATH=$therockPath" >> $env:GITHUB_ENV
          echo "CMAKE_PREFIX_PATH=$therockPath" >> $env:GITHUB_ENV
          echo "HIP_DEVICE_LIB_PATH=$therockPath\lib\llvm\amdgcn\bitcode" >> $env:GITHUB_ENV
          echo "HIP_PLATFORM=amd" >> $env:GITHUB_ENV
          echo "LLVM_PATH=$therockPath\lib\llvm" >> $env:GITHUB_ENV
          echo "$therockPath\bin" >> $env:GITHUB_PATH

      - name: Verify TheRock compiler
        run: |
          & "${env:HIP_PATH}\lib\llvm\bin\clang.exe" --version

      - name: ccache
        uses: ggml-org/ccache-action@v1.2.21
        with:
          # TODO: this build does not match the build in release.yml, so we use a different cache key
          #       ideally, the builds should match, similar to the CUDA build above so that we would be able
          #       to populate the ccache for the release with manual runs of this workflow
          #key: release-windows-2022-x64-hip-${{ env.ROCM_VERSION }}-${{ matrix.name }}
          key:  windows-rocm-${{ matrix.ROCM_VERSION }}-${{ matrix.build }}
          evict-old-files: 1d
          max-size: "1G"

      - name: Build
        id: cmake_build
        run: |
          cmake -S . -B build `
            -G "Ninja Multi-Config" `
            -DCMAKE_PREFIX_PATH="${env:HIP_PATH}" `
            -DGGML_BACKEND_DL=ON `
            -DGGML_NATIVE=OFF `
            -DGGML_CPU=OFF `
            -DGGML_HIP=ON `
            -DCMAKE_C_COMPILER="${env:HIP_PATH}\lib\llvm\bin\clang.exe" `
            -DCMAKE_CXX_COMPILER="${env:HIP_PATH}\lib\llvm\bin\clang++.exe" `
            -DCMAKE_C_FLAGS="-Wno-error=incompatible-pointer-types" `
            -DCMAKE_HIP_COMPILER="${env:HIP_PATH}\lib\llvm\bin\clang.exe" `
            -DHIP_PATH="${env:HIP_PATH}" `
            -DAMDGPU_TARGETS="${{ matrix.gpu_targets }}"
          cmake --build build --config Release --parallel ${env:NUMBER_OF_PROCESSORS}

      - name: Verify HIP backend was built
        run: |
          $hipDll = Get-ChildItem -Path build\bin\Release -Filter "ggml-hip*.dll" -ErrorAction SilentlyContinue
          if (-not $hipDll) {
            Write-Host "##[error]ggml-hip*.dll was NOT produced. The HIP backend silently failed to build."
            Write-Host "Contents of build\bin\Release:"
            Get-ChildItem build\bin\Release | Format-Table -AutoSize
            exit 1
          }
          Write-Host "HIP backend artifact found:"
          $hipDll | Format-Table FullName, Length -AutoSize

      - name: Determine tag name
        id: tag
        uses: ./.github/actions/get-tag-name

      - name: Get ROCm short version
        run: |
          $rocmVersionShort = ('${{ matrix.ROCM_VERSION }}'.Split('.')[0..1] -join '.')
          echo "ROCM_VERSION_SHORT=$rocmVersionShort" >> $env:GITHUB_ENV

      - name: Bundle TheRock runtime DLLs
        run: |
          $ErrorActionPreference = "Stop"
          $therockBin = "${env:HIP_PATH}\bin"
          $dest = "build\bin\Release"
          
          @("amdhip64_7.dll", "rocm_kpack.dll", "amd_comgr.dll") | ForEach-Object {
            $src = Join-Path $therockBin $_
            if (Test-Path $src) {
              Copy-Item $src $dest -Force
              Write-Host "Copied $_"
            } else {
              Write-Host "Warning: $_ not found"
            }
          }
          
          @("rocblas.dll", "hipblas.dll") | ForEach-Object {
            $src = Join-Path $therockBin $_
            if (Test-Path $src) {
              Copy-Item $src $dest -Force
              Write-Host "Copied $_"
            } else {
              Write-Host "Warning: $_ not found"
            }
          }


      - name: Get ROCm short version
        run: |
          $rocmVersionShort = ('${{ matrix.ROCM_VERSION }}'.Split('.')[0..1] -join '.')
          echo "ROCM_VERSION_SHORT=$rocmVersionShort" >> $env:GITHUB_ENV
          
      - name: Pack artifacts
        id: pack_artifacts
        run: |
          7z a -snl lama-bin-win-rocm-${{ env.ROCM_VERSION_SHORT }}-${{ matrix.build }}.zip .\build\bin\Release\*

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          path: lama-bin-win-rocm-${{ env.ROCM_VERSION_SHORT }}-${{ matrix.build }}.zip
          name: lama-bin-win-rocm-${{ env.ROCM_VERSION_SHORT }}-${{ matrix.build }}.zip

      - name: ccache-clear
        uses: ./.github/actions/ccache-clear
        with:
          key: windows-rocm-${{ matrix.ROCM_VERSION }}-${{ matrix.build }}
